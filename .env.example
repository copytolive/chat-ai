# Legacy-compatible HTTP port. Local-only by default.
PORT=3847
HOST=127.0.0.1

# Required by the service when HOST is changed to a non-loopback interface.
# The scanner UI will prompt for this token and keep it only in sessionStorage.
SCANNER_TOKEN=

# Optional admin-only reconnect token. If blank, POST /reconnect is disabled.
ADMIN_TOKEN=

# Keep auth outside the repository in production, e.g. /var/lib/chat-ai/wa-auth
WA_AUTH_DIR=.auth/whatsapp
WA_REPLY_GROUPS=false
# Optional comma-separated exact JIDs, e.g. 628123456789@s.whatsapp.net
WA_ALLOWED_JIDS=

# AI adapter
AI_ENABLED=false
# OpenAI-compatible base URL WITHOUT /chat/completions.
# Example local server: http://127.0.0.1:11434/v1
AI_BASE_URL=http://127.0.0.1:11434/v1
AI_MODEL=
AI_API_KEY=
AI_TIMEOUT_MS=60000
SYSTEM_PROMPT=You are a helpful WhatsApp assistant. Reply clearly and concisely.

# Reply controls
MAX_INPUT_CHARS=6000
MAX_REPLY_CHARS=6000
